export interface datosWeb {
    name: string;
    range: string;
    gmail: String;
    url: String;
}

export interface datosMovil {
    name: string;
    range: string;
    gmail: String;
    url: String;
}